import React from 'react'
import styles from './styles.module.css'

const Footer = () => {
  return (
    <div className={`container ${styles.container}`}>
    <footer className={`${styles.wrapper}`}>
      <h4>Joel Rondinel Pacheco</h4>
      <div>
        <h4>Icons</h4>
      </div>
    </footer>
    </div>
  )
}

export default Footer